language: rust

addons:
  apt:
    packages:
      - gfortran

rust:
  - "1.26.2"
  - stable
  - beta
  - nightly

sudo: false

cache: cargo

install:
  - cargo build

script:
  - cargo test

compiler:
    - clang
