language: rust

env: TYPE=default RUST_BACKTRACE=1

addons:
  apt:
    packages:
      - gfortran

rust:
  - "1.27.2"
  - stable
  - beta
  - nightly

sudo: false

cache: cargo

install:
  - cargo build -v

script:
  - cargo test -v

compiler:
    - clang
